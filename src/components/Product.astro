---
const { name, image, text } = Astro.props;
---
<section class="w-full px-4 py-10">
    <img src={ image } alt={ name } />
    <h1 class="my-4 font-bold text-2xl">{ name }</h1>
    <p class="text-lg">{ text }</p>
</section>